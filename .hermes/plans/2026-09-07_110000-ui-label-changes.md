# Plan: UI Label Changes — RAG → จัดลำดับเนื้อหา + Relations wording

## Changes from PDF doc + user request

---

## Task 1: เปลี่ยน "RAG" → "จัดลำดับเนื้อหา" ใน UI ทุกที่

| File | Line | เดิม | ใหม่ |
|------|------|------|------|
| `components/edit/EditHubWorkspace.vue` | 409 | `จัดลำดับ RAG` | `จัดลำดับเนื้อหา` |
| `components/edit/EditHubWorkspace.vue` | 410 | `จัดกลุ่ม เลือก และเรียงบล็อกก่อนบันทึกเข้าคลัง RAG` | `จัดกลุ่ม เลือก และเรียงบล็อกเนื้อหาก่อนเผยแพร่` |
| `components/edit/EditHubWorkspace.vue` | 494 | `ยังไม่ได้จัดลำดับ RAG` | `ยังไม่ได้จัดลำดับเนื้อหา` |
| `components/edit/EditHubWorkspace.vue` | 495 | `ต้องจัดลำดับเนื้อหา RAG ให้เสร็จก่อนเผยแพร่` | `ต้องจัดลำดับเนื้อหาให้เสร็จก่อนเผยแพร่` |
| `components/edit/EditHubWorkspace.vue` | 497 | `ไปจัดลำดับ RAG` | `ไปจัดลำดับเนื้อหา` |
| `components/shared/PublishConfirmDialog.vue` | 137 | `จัดลำดับ RAG` | `จัดลำดับเนื้อหา` |
| `components/shared/PublishConfirmDialog.vue` | 138-139 | `ยังไม่ได้จัดลำดับ` | `ยังไม่ได้จัดลำดับเนื้อหา` |
| `composables/usePublishGates.ts` | (ทุกที่ที่มี RAG ใน label/status) | `จัดลำดับ RAG` | `จัดลำดับเนื้อหา` |

> หมายเหตุ: code variable ชื่อ `rag`, `ragOk`, `rag_skipped` ไม่ต้องเปลี่ยน — เปลี่ยนเฉพาะ user-facing text

---

## Task 2: เปลี่ยน label ความสัมพันธ์กฎหมาย (จาก PDF doc)

### 2a. `pages/law-relations/LawRelationsPage.vue`

| เดิม | ใหม่ |
|------|------|
| `ลำดับชั้นเอกสาร` (line 44) | `ความสัมพันธ์กฎหมาย` |
| `กฎหมายใหม่ระบุได้แค่ลำดับชั้นเอกสาร (กฎหมายแม่) ไม่ผูกฉบับขั้นเดียวกัน` (line 279) | `ระบุความสัมพันธ์ของข้อบังคับ ระเบียบ หรือประกาศที่มีระดับสูงกว่าหรือระดับเดียวกัน` |

### 2b. `components/shared/LawRelationColumnPicker.vue`

| เดิม | ใหม่ |
|------|------|
| `เลือกกฎหมายเป้าหมาย` (line 15) | `เลือกข้อบังคับ ระเบียบ ประกาศที่ต้องการอ้างอิง` |
| `กฎหมายแม่และชั้นเดียวกัน` (line 192) | `กฎหมายระดับสูงกว่าหรือระดับเดียวกัน` |
| `กฎหมายแม่` (line 194, catalogMode=parents) | `กฎหมายระดับสูงกว่า` |

---

## Verification
```bash
cd apps/app-laravel && npm run typecheck
```
Exit 0 expected.

## Commit
```
fix(ui): rename RAG → จัดลำดับเนื้อหา in user-facing labels
fix(relations): update relation wording per doc spec
```
