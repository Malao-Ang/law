services:
  laravel-app:
    build:
      context: ./apps/app-laravel
      dockerfile: Dockerfile
    container_name: thai-ocr-laravel-app
    working_dir: /var/www/html
    command: >
      sh -c "git config --global --add safe.directory '*'
      && composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev
      && php artisan config:clear
      && php artisan serve --host=0.0.0.0 --port=8000 --no-reload"
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    volumes:
      - ./apps/app-laravel:/var/www/html
      - laravel_vendor:/var/www/html/vendor
      - poc_storage:/var/www/html/storage/app/poc
    env_file:
      - .env
    depends_on:
      - redis
      - ocr-service
    ports:
      - "8000:8000"

  laravel-vite:
    image: node:20-bookworm-slim
    container_name: thai-ocr-laravel-vite
    working_dir: /var/www/html
    command: >
      sh -c "if [ ! -f node_modules/.bin/vite ]; then
        echo 'ERROR: node_modules/.bin/vite missing. Run \"npm install\" in apps/app-laravel on the host first.' >&2;
        exit 1;
      fi
      && npm run dev"
    environment:
      - HMR_HOST=localhost
    volumes:
      - ./apps/app-laravel:/var/www/html
    ports:
      - "5173:5173"

  queue-worker:
    build:
      context: ./apps/app-laravel
      dockerfile: Dockerfile
    container_name: thai-ocr-queue-worker
    working_dir: /var/www/html
    command: >
      sh -c "git config --global --add safe.directory '*'
      && composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev
      && php artisan queue:listen --tries=1 --timeout=1800"
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    volumes:
      - ./apps/app-laravel:/var/www/html
      - laravel_vendor:/var/www/html/vendor
      - poc_storage:/var/www/html/storage/app/poc
    env_file:
      - .env
    depends_on:
      - redis
      - laravel-app
      - ocr-service

  ocr-service:
    build:
      context: ./apps/ocr-service
      dockerfile: Dockerfile
    container_name: thai-ocr-service
    working_dir: /app
    volumes:
      - ./apps/ocr-service:/app
      - poc_storage:/data/poc
    env_file:
      - .env
    ports:
      - "8010:8010"

  redis:
    image: redis:7-alpine
    container_name: thai-ocr-redis
    ports:
      - "6379:6379"

volumes:
  poc_storage:
  laravel_vendor:


