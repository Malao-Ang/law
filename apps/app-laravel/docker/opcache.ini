; Development only: recheck file mtimes on every request so bind-mounted PHP
; changes are visible without rebuilding or restarting the container.
opcache.enable=1
opcache.validate_timestamps=1
opcache.revalidate_freq=0
opcache.memory_consumption=256
opcache.max_accelerated_files=20000
opcache.interned_strings_buffer=16
