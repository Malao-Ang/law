

# 🔥 PROMPT สำหรับ AI ตรวจสอบระบบ Word/PDF → HTML → CKEditor 5

---

## 🎯 Objective

ฉันกำลังพัฒนา Laravel + Vue 3 + CKEditor 5 ระบบแปลงเอกสาร Word/PDF เป็น HTML แล้วเปิดแก้ไขใน Editor

ต้องการให้คุณตรวจสอบ architecture และแก้โค้ดให้ถูกต้อง โดยมีเงื่อนไขดังนี้:

---

## 📌 สิ่งที่ทำมาแล้ว

### 1️⃣ เปลี่ยนจาก Tiptap → CKEditor 5

เดิม:

* ใช้ Tiptap
* มีปัญหา table render ไม่ครบ
* Table schema strict
* PDF layout พัง

ปัจจุบัน:

* เปลี่ยนเป็น CKEditor 5 (Classic build)
* เปิด htmlSupport allow all
* เปิด pasteFromOffice keepFormat
* ใช้ insertTable + mergeTableCells

---

### 2️⃣ Backend Laravel แยก Service Layer

โครงสร้างปัจจุบัน:

```
WordController
  → DocumentConvertService
      → DocxToHtmlConverter
      → PdfToHtmlConverter
```

---

### 3️⃣ Requirement ที่ต้องรองรับ

### ✅ Word (.docx)

ต้องรองรับ:

* ตาราง (table)
* row merge (vMerge)
* colspan (gridSpan)
* bold / italic
* center heading
* numbering
* indentation
* Thai layout 100%
* preserve table structure

---

### ✅ PDF

ต้อง:

* อ่าน text
* ตรวจ indent
* ตรวจ center
* ตรวจ numbering
* ถ้าเป็น table ให้พยายาม detect table structure

---

### 4️⃣ แสดงผลบนหน้าจอ 2 รูปแบบ

1️⃣ แสดงใน CKEditor (Editable mode)
2️⃣ แสดงผล HTML Raw (Debug mode)

ต้องสามารถ toggle ได้

---

# ❗ ปัญหาปัจจุบัน

* ตารางบางไฟล์ยัง format ไม่ครบ
* numbering ไม่ครบทุก level
* indent บาง paragraph ไม่แม่น
* บาง element null แล้ว attr() crash
* PDF layout ยังไม่ 100%

---

# 📦 สิ่งที่ต้องการให้ AI ทำ

## 1️⃣ ตรวจ Architecture

* Controller ควรทำหน้าที่อะไร
* Service layer ควรแยกอะไรบ้าง
* ควรมี Strategy Pattern หรือไม่
* ควรแยก Layout Engine ออกมาหรือไม่

---

## 2️⃣ แก้ DocxToHtmlConverter ให้ Production Ready

ต้องรองรับ:

* w:tbl
* w:tr
* w:tc
* w:vMerge
* w:gridSpan
* w:pPr/w:jc
* w:ind
* w:numPr
* w:rPr bold italic size
* numbering.xml mapping

ต้องไม่ crash ถ้า element ไม่มี

---

## 3️⃣ แก้ DOMXPath safe helper

ต้องสร้าง helper แบบนี้:

```php
private function el(?\DOMNodeList $list): ?\DOMElement
{
    return ($list instanceof \DOMNodeList && $list->length > 0 && $list->item(0) instanceof \DOMElement)
        ? $list->item(0)
        : null;
}
```

และ attr:

```php
private function attr(?\DOMElement $el, string $name): ?string
{
    return $el?->getAttribute($name) ?: null;
}
```

---

## 4️⃣ แก้ numbering mapping 100%

โหลดจาก numbering.xml

map:

```
abstractNum → lvl
numId → abstractNumId
ilvl → format
```

---

## 5️⃣ PDF Conversion Strategy

ถ้าจะ production จริง:

* ใช้ pdf2htmlEX ?
* หรือใช้ layout-based parsing?
* หรือ AI merge text?

แนะนำวิธีที่เหมาะสม

---

## 6️⃣ Frontend Structure

Vue + CKEditor:

ต้อง:

* toggle editor / preview / raw html
* prevent memory leak
* destroy editor before re-init
* allow all html

---

# 📎 ตัวอย่างไฟล์

ฉันกำลังทดสอบไฟล์:

* ประกาศ บริการผลิตสื่อฯ (2).docx
* มี table + numbering + Thai layout

---

# 🎯 สิ่งที่ต้องการจาก AI

กรุณา:

1. วิเคราะห์ architecture ปัจจุบัน
2. เสนอ refactor ที่เหมาะสม
3. แก้ DocxToHtmlConverter ให้ครบ feature
4. แนะนำ PDF strategy
5. ให้โค้ดตัวอย่างที่ production ready

---

# 🔥 Output Format ที่ต้องการ

ตอบกลับแบบนี้:

```
1. Architecture Analysis
2. Critical Fixes
3. Refactored Service Design
4. Production Docx Engine Code
5. PDF Strategy Recommendation
6. Frontend CKEditor Best Practice
```

---

# 🚀 Extra

ถ้าเป็นไปได้ ช่วยออกแบบ conversion engine แบบ enterprise-grade ที่:

* ไม่พังเมื่อ element หาย
* รองรับ Thai document
* รองรับราชการ format
* รองรับ table merge
* รองรับ numbering nested
* รองรับ heading detection

---

## End of Prompt

---

# 🧠 คำแนะนำ

ถ้าใช้ Cursor:

* paste prompt นี้
* paste DocxToHtmlConverter class
* paste WordController
* ให้ AI rewrite ทั้งระบบ

---

## 6) สิ่งที่เพิ่มเติมเพื่อการใช้งานจริง (Implemented) - [Added 2026-02-15]

จากการวิเคราะห์และพัฒนา พบว่าแผนเดิม (Custom Parser) เหมาะสมที่สุดสำหรับการจัดรูปแบบเอกสารราชการไทย (Thai Government Document Layout) แต่ยังขาดฟีเจอร์สำคัญบางประการ จึงได้ทำการพัฒนาเพิ่มเติมดังนี้:

### 1. รองรับรูปภาพ (Image Support) 🖼️
- **ปัญหาเดิม:** Custom XML Parser ปกติจะข้าม tag รูปภาพ ทำให้โลโก้ครุฑหรือลายเซ็นหายไป
- **สิ่งที่พัฒนาเพิ่ม:** 
  - เพิ่มฟังก์ชัน `parseDrawing()` ใน `DocxToHtmlConverter`
  - อ่านไฟล์ `word/_rels/document.xml.rels` เพื่อ map ความสัมพันธ์ของไฟล์
  - ดึงไฟล์รูปภาพจาก folder `word/media/` ใน Zip
  - แปลงรูปภาพเป็น **Base64** และฝังลงใน HTML (`<img src="data:image/...">`) ทำให้แสดงผลได้ทันทีโดยไม่ต้องเก็บไฟล์แยก

### 2. ความทนทานและตรวจสอบข้อผิดพลาด (Robustness & Logging) 🛡️
- **ปัญหาเดิม:** หากไฟล์เสียหรือ format ผิดโปรแกรมจะ crash
- **สิ่งที่พัฒนาเพิ่ม:**
  - เพิ่ม `try-catch` block ครอบการทำงานทั้งหมดใน `DocumentConvertService`
  - บันทึก Error Log อย่างละเอียด (Stack Trace) ผ่าน `Log::error()` เพื่อให้ทีมพัฒนาหาสาเหตุได้ง่าย
  - เพิ่มการตรวจสอบไฟล์ (`file_exists`, Validation) ก่อนเริ่มกระบวนการ

### 3. ความปลอดภัย (Security) 🔒
- **XSS Protection:** ใช้ `htmlspecialchars()` กับทุกข้อความที่ดึงมาจาก XML เพื่อป้องกันการฝัง Script
- **Safe Rendering:** รูปภาพถูก render ผ่าน Base64 ที่สร้างเอง ไม่ได้รับ URL จากภายนอกโดยตรง

### สถานะปัจจุบัน (Current Status)
- ✅ **DOCX Text & Formatting:** สมบูรณ์ (รองรับย่อหน้าไทย, ข้อ ๑, ตาราง)
- ✅ **DOCX Images:** ใช้งานได้แล้ว
- ✅ **PDF Conversion:** ใช้งานได้ (ผ่าน `smalot/pdfparser` + Custom Heuristics)
- ✅ **System Integration:** พร้อมใช้งานผ่าน Controller และ Service Layer
